package com.leave;

import java.time.LocalDate;
import java.time.format.DateTimeFormatter;
import java.time.temporal.ChronoUnit;

public class VacationLeaveRequest extends LeaveRequest {
	private boolean planned;

	
	public VacationLeaveRequest(int requestId, Employee employee, String startDate, String endDate,String deliveryDate,boolean planned) {
		super(requestId,employee,startDate,endDate,"Vacation Leave");
		this.planned = planned;
	}
	
	public boolean isPlanned() {
		return planned;
	}
	
	@Override
	public boolean processRequest() {
	    if (!planned) {
	        System.out.println("Maternity Date is not provided!");
	        return false;
	    }
	    System.out.println("Processing maternity leave request...");
	    return true;
	}

	@Override
	public boolean approve(String approverName) {
		// TODO Auto-generated method stub
		return false;
	}

	@Override
	public boolean deny(String approverName, String reason) {
		// TODO Auto-generated method stub
		return false;
	}

	@Override
	public int calculateLeaveDays() {
		DateTimeFormatter formatter = DateTimeFormatter.ofPattern("yyyy-mm-dd");
		LocalDate start = LocalDate.parse(startDate,formatter);
		LocalDate end = LocalDate.parse(endDate,formatter);
		return (int) ChronoUnit.DAYS.between(start, end);
	}
}
